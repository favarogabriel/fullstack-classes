const nomes = ['Maria', 'João', 'Eduardo', 'Gabriel', 'Julia'];
console.log(nomes);

// Unshift
// nomes.splice(0, 0, "Robervau");

// Push
// nomes.splice(nomes.length, 0, 'Roberto');

// nomes.splice(índice, delete, elem1+, elem2+)
// const removidos = nomes.splice(3, 0, 'Luiz');

// Shift
// const removidos = nomes.splice(0, 1);
